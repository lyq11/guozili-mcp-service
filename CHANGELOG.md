# 更新日志

## 0.5.2 - 2026-07-21

- 原理图缓存新增项目级网络索引，从真实导线、网络端口和网络标签汇总网络名。
- 原理图—PCB 一致性检查改用项目网络索引，修复原理图网络被错误报告为 0 的问题。

## 0.5.1 - 2026-07-21

- 修复 EasyEDA 启动时没有原理图焦点会把 `[backup]` 原理图一起加入缓存的问题；存在 `[main]` 时始终只缓存其图页。
- PCB 板框检查新增 Board Outline 层 Polyline 支持，板外器件检查可使用折线板框的真实 BBox。
- PCB 局部区域检查兼容 EasyEDA 的反向 Y 参数约定，并向 MCP 返回统一的 minY/maxY 边界。

## 0.5.0 - 2026-07-21

- 初始化时只缓存关联 `[main]` 原理图的 Board，保存板框、层叠、封装、焊盘、网络、走线、过孔、铺铜、区域和设计规则。
- 新增 PCB 列表、整板/局部检查、未布线网络、封装重叠、板外器件、原理图一致性和原生 DRC 工具。
- 新增器件移动/旋转/锁定、成组布局、走线、过孔、铺铜及铺铜重建工具；线宽、层、网络和过孔尺寸必须显式提供。
- 新增从 `[main]` 原理图导入 PCB 变更，并检查缺失/重复位号、封装、BOM 型号及网络差异。
- 每个 MCP 会话首次修改一块 PCB 前自动创建游离 `[backup]` PCB；确定性修复当前仅重建铺铜，不自动更改电气规则。

## 0.4.20 - 2026-07-21

- `schematic_inspect_page` 现在返回原生网络标签的 ID、网络名和坐标。
- 新增 `schematic_update_net_labels`，可将游离网络标签移动到目标导线并按需改名。
- 写入前验证目标确实是 EasyEDA 原生 `netlabel` 图元，避免误改普通元件属性。

## 0.4.11 - 2026-07-20

- 新增原理图器件原生属性修改工具，可写入并显示 `Value` 等属性。
- 删除图元操作新增文字图元支持，便于移除临时标注。
- 页面检查结果新增器件属性明细，支持写入前后核对。

## 0.4.10 - 2026-07-20

- 修正 EasyEDA 原理图 Y 轴方向：视觉右下角标题栏应映射到低 Y 区域。

## 0.4.9 - 2026-07-20

- 将原始图元覆盖率与实际放置禁区率分开，避免用导线笔画面积误判页面空闲程度。
- 器件、端口和导线分别按可配置安全间距膨胀，并预留图框边缘与右下标题栏。
- 空白区域未指定偏好位置时默认从画布中心附近搜索，不再偏向左上角。

## 0.4.8 - 2026-07-20

- 修复页面图框 `sheet` 被当作器件占用，导致网格占用率接近 100% 的问题。
- 器件 BBox 合并属性前强制按 `parentPrimitiveId` 过滤，避免其它页面属性把边界拉到原点。
- 增加异常 BBox 可信度检查；边界相对器件原点和 PIN 跨度明显失真时自动回退到局部几何范围。
- 健康检查新增扩展版本，能力协商新增 `operations.validate`，便于识别服务与扩展版本不匹配。
- 扩展版本升级到 0.2.8。

## 0.4.7 - 2026-07-20

- 新增页面二维占用缓存，默认将 1635×1160 画布按 5×5 单元映射为约 327×232 网格。
- 新增 `schematic_get_page_occupancy` 和 `schematic_find_free_regions`，支持占用摘要、压缩行和空白矩形搜索。
- 端口规划接入器件、导线、端口和计划占用分层网格，并在写入后自动失效缓存。
- 端口与连接导线合并为插件侧可补偿操作；实测矩形冲突或画线失败时删除已创建图元。
- 写入前新增全批操作预检；扩展版本升级到 0.2.7。

## 0.4.6 - 2026-07-20

- 端口规划由连接点模型升级为完整矩形模型，覆盖端口符号、方向标记和网络名宽度。
- 已有器件碰撞检测改用 EasyEDA 真实图元与可见属性联合 BBox，源器件也参与端口矩形检测。
- 页面检查新增器件 `bbox`，引脚新增 `pinType` 与 `rotation`。
- 扩展版本升级到 0.2.6。

## 0.4.5 - 2026-07-20

- `schematic_create_ports_for_pins` 新增障碍感知正交布线，会避让器件、已有导线及同批次新增端口。
- 端口规划会比较直出、延长和多通道绕行候选，并返回路径评分与冲突统计。
- 没有无冲突路径时中止写入，避免强行放置造成重叠或跨网交叉。

## 0.4.4 - 2026-07-20

- 新增 `schematic_transform_components`，可批量移动、旋转和镜像已有普通器件。
- `schematic_inspect_page` 现在返回器件的 `mirror` 状态。
- 器件变换结果返回器件及其引脚的新旧坐标，便于识别需要重接的导线。
- 扩展版本升级到 0.2.5。

## 0.4.3 - 2026-07-20

- 新增 `schematic_set_no_connects`，可按器件与引脚号批量设置或取消 EasyEDA 原生非连接标识。
- 新增底层白名单操作 `set_no_connects`，直接修改引脚 `noConnected` 状态。
- `schematic_inspect_page` 现在返回每个引脚的 `noConnected` 布尔值，便于修改前后核对。
- 扩展版本升级到 0.2.4。

## 0.4.2 - 2026-07-20

- 新增 `schematic_move_components_with_wires`，可按统一偏移量成组平移选定器件与导线全部拐点。
- 新增底层白名单操作 `translate_group`，在插件侧完成整组移动并只保存一次。
- 工具不会自动选择导线，避免误移动仍连接静止器件的网络。
- 扩展版本升级到 0.2.2。

## 0.4.1 - 2026-07-20

- `schematic_list_pages` 改为读取工程级文档树，没有打开图页时也能列出全部原理图和图页。
- 新增 `schematic_move_components`，可按图元 ID 批量移动既有普通器件到绝对坐标。
- 工程级操作校验不再依赖当前活动原理图。
- 扩展版本升级到 0.2.1。

## 0.4.0 - 2026-07-20

- 项目更名为“果子狸MCP服务”。
- 补充完整的本地 MCP 安装、配置、构建和排错说明。
- 扩展发布版本升级到 0.2.0，并加入发布所需元数据、原创图标、README 和 CHANGELOG。
- 保留 17 个结构化 MCP 工具、会话首次写入自动备份、PIN 布局识别和可读性分析能力。

## 0.3.0 - 2026-07-20

- 将常用原理图操作封装为直接 MCP 工具。
- 新增按器件和 PIN 几何方向创建端口的布局逻辑。
- 写操作调整为会话首次修改前自动备份一次。
- 新增编辑器连接状态悬浮框。
